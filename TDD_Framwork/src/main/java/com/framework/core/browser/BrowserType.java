package com.framework.core.browser;

public enum BrowserType
{
    //first we will check if particular browser is present here or not we will through the error
    // invalid browser if requested browse is not in the list
    CHROME,
    INTERNETEXPLORE,
    EDGE,
    FIREFOX
}
